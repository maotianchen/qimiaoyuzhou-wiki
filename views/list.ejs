<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= heading %> - 奇喵宇宙维基</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="icon" href="/logo.png">
    <style>
        body { font-family: sans-serif; background-color: #f6f6f6; }
        .wiki-serif { font-family: 'Linux Libertine', 'Georgia', 'Times', serif; }
        .link-blue { color: #0645ad; text-decoration: none; }
        .link-blue:hover { text-decoration: underline; }
    </style>
</head>
<body class="text-gray-800 text-sm leading-6">

    <nav class="bg-white border-b border-gray-300 sticky top-0 z-50 h-14">
        <div class="container mx-auto px-4 h-full flex items-center">
            <a href="/" class="flex items-center gap-2 group">
                <div class="w-10 h-10 rounded-full border border-gray-300 overflow-hidden">
                    <img src="/logo.png" alt="奇喵宇宙" class="w-full h-full object-cover">
                </div>
                <span class="wiki-serif text-lg font-bold leading-none">奇喵宇宙维基</span>
            </a>
        </div>
    </nav>

    <div class="container mx-auto px-4 py-4 max-w-4xl">
        <main class="bg-white border border-gray-300 p-4 md:p-6 shadow-sm">
            <h1 class="wiki-serif text-2xl border-b border-gray-300 pb-1 mb-4"><%= heading %></h1>

            <% if (!items.length) { %>
                <p class="text-gray-500"><%= emptyText %></p>
            <% } else { %>
                <ul class="divide-y divide-gray-200">
                    <% items.forEach(function(item) { %>
                    <li class="py-2 flex items-baseline justify-between gap-4">
                        <div class="min-w-0">
                            <a href="/wiki/<%= encodeURIComponent(item.title) %>" class="link-blue font-bold"><%= item.title %></a>
                            <% if (item.sub) { %>
                            <span class="text-gray-500 text-xs">(<%= item.sub %>)</span>
                            <% } %>
                            <% if (item.author) { %>
                            <span class="text-gray-400 text-xs">by <%= item.author %></span>
                            <% } %>
                        </div>
                        <% if (item.time) { %>
                        <span class="text-gray-400 text-xs whitespace-nowrap">
                            <%= new Date(item.time).toLocaleString('zh-CN', { timeZone: 'Asia/Shanghai' }) %>
                        </span>
                        <% } %>
                    </li>
                    <% }) %>
                </ul>
            <% } %>
        </main>
    </div>

    <footer class="mt-8 py-6 bg-white border-t border-gray-300 text-xs text-gray-600">
        <div class="container mx-auto px-4 text-center">
            <p>奇喵宇宙维基</p>
        </div>
    </footer>
</body>
</html>
